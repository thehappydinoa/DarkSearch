[build-system]
requires = ["setuptools", "wheel"]

[tool.black]
line-length = 100
target-version = ["py37"]

[tool.pytest.ini_options]
minversion = "4.0"
script_launch_mode = "subprocess"
testpaths = "tests"
